# a0006 サバイバー系アクションゲーム

## このプロジェクトについて

ブラウザで動くローグライト・サバイバー系ゲーム。PC・スマホ両対応。

**仕様の唯一の正は `docs/spec.md`。** 実装前に必ず該当章を読むこと。仕様書に書かれていないことを推測で実装しない。

## 技術スタック（変更禁止）

- TypeScript 5.x（`strict: true`）
- Vite 8.x
- PixiJS **8.x**（v7 以前の書き方は使わない。`docs/spec.md` §18 を必ず参照）
- 物理・当たり判定・状態管理はすべて自前実装

**新しい npm パッケージを追加しない。** 必要だと判断した場合は、実装を止めて理由とともに提案すること。

## コーディング規約

- `any` 禁止。型は `src/types/index.ts` に集約
- **毎フレームの `new` 禁止。** 敵・弾・ジェム・パーティクルは必ずオブジェクトプールから取得し、死亡時に返却する
- 配列からの削除は `splice` ではなく swap-remove（末尾と入れ替えて `pop`）
- 距離比較は平方根を取らず二乗で比較する
- **色は `src/ui/theme.ts` の `COLORS` のみ使用。** ハードコードした色コードを書かない
- ゲームロジックは入力方式（キーボード／タッチ）を知らない。`InputState` 経由で抽象化する
- バランス数値はコードに埋め込まず `src/data/*.json` に置く
- コメントは日本語。「何をしているか」ではなく「なぜそうしているか」を書く

## 作業の進め方

- **1回の指示で1フェーズだけ実装する。** `docs/spec.md` §19 のフェーズ分割に従い、指示されていないフェーズには着手しない
- 各フェーズの受け入れ基準をすべて満たしたら、チェックリスト形式で確認結果を報告する
- 受け入れ基準のチェックは、実際に検証した項目のみ `[x]` を付ける。未検証の項目は `[ ]` のまま残し、何が未検証かを明記すること
- 仕様書に曖昧な点や矛盾を見つけたら、実装を進めずに質問する
- 仕様にない機能を「あった方がいい」と判断して追加しない
- **バランスに影響する変更（`src/data/*.json`、武器・敵・EXP・コインのロジック）の後は
  `npm run baseline` を実行し、差分を報告すること。** 計測はセーブを毎回明示注入する
  （localStorage の暗黙引き継ぎで基準値が汚染された事故があったため、手動検証でも必ず注入する）

## コマンド

```bash
npm run dev        # 開発サーバー
npm run build      # 本番ビルド
npm run typecheck  # 型チェック（tsc --noEmit）
```

## 応答

日本語で、簡潔に。
